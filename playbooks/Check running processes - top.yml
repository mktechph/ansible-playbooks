---
- hosts: all
  gather_facts: yes

  tasks:
    - name: "Check Running processes using top"
      ansible.builtin.shell:
        cmd: ls
      register: processes

    - debug:
        var: processes
   